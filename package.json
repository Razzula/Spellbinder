{
    "name": "spellbinder-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "bun --cwd packages/spellbinder dev",
        "build": "bun --cwd packages/spellbinder build"
    }
}
